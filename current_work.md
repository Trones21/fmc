creating a transformation property to take one key apply some transformation and then make another key from it

Note: I do NOT need to do this for the slug. Docusaurus will simply append whatever value you have if its a relative path (so therefore I dont need to caclulate the slug paths)